# On-Chain-Inheritance-Project
a smart contract system that automatically releases a person's crypto assets to their designated heirs if they stop interacting with the blockchain for a configurable period of time. The core concept is called a dead man's switch: a mechanism that activates when someone goes silent, assuming the worst.
